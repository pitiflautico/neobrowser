# Borrador Twitter/X — hilo (usuario publica)

**Formato**: hilo de 5 tweets + gif adjunto en el primero (`docs/assets/demo.gif`, 89 KB, sube directo).
**Cuándo**: entre semana 15-17h CET (9-11am ET).

---

**Tweet 1** (adjuntar demo.gif):
```
Your AI agent keeps hitting login walls and bot checks because every browser MCP launches a fresh, fingerprintable headless browser.

NeoBrowser drives your REAL Chrome — with your real logged-in sessions.

Open source, single 6.4 MB Rust binary 🧵
```

**Tweet 2**:
```
The stealth isn't spoofed — that's why it works.

Real UA matching its own Client Hints. Real GPU WebGL. navigator.webdriver actually gone. Passes bot.sannysoft with the host's genuine fingerprint, verified live in CI.
```

**Tweet 3**:
```
Sessions: opt-in cookie import from your real Chrome profile (macOS Keychain / Linux secret-service / Windows DPAPI).

Identity cookies excluded so your browser never gets logged out. Files written 0600. Everything auditable, MIT.
```

**Tweet 4**:
```
And it knows its limits: when a site throws reCAPTCHA/Turnstile, it doesn't pretend to be invisible — it detects the wall and tells the model how to react.

67 tools: multi-tab, forms, upload/download, search, record/replay playbooks.
```

**Tweet 5**:
```
Honest benchmark vs Playwright MCP included (nothing tuned to make either win): Playwright is faster; NeoBrowser does session persistence + uploads it can't.

⭐ https://github.com/pitiflautico/neobrowser
```

## Variante post único (si no quieres hilo)
```
NeoBrowser: an MCP server that drives your real Chrome with your real logged-in sessions. Genuine fingerprint (not spoofed), human-cadence clicks, bot-wall detection, 67 tools, one 6.4 MB Rust binary. Honest benchmark vs Playwright MCP in the repo. https://github.com/pitiflautico/neobrowser
```
