# NeoBrowser tools (67)

Generated by `neobrowser tools --markdown`.

## `status`

Report browser status: Chrome binary discovery and whether a live session exists.

_No parameters._

## `observe`

Accessibility snapshot with STABLE element references you can pass to click/type. Prefer this over `find` for multi-step work: a reference like `button:Continue#0` survives a re-render, while a backendNodeId does not. Pass diff=true to get only what changed since the last observe, which is usually a few lines instead of the whole tree.

| param | type | required | description |
|---|---|---|---|
| `budget_chars` | integer |  | Maximum characters of listing to return (default 4000). Truncation is reported, never silent |
| `diff` | boolean |  | Return only added/removed/changed elements since the previous observe (default false) |
| `mode` | string |  | interactive (default, actionable elements only) | visible (adds static text) | full (everything, for debugging) |

## `revoke_session`

Destroy this profile's stored session material (cookie vault, localStorage, manifest). Overwrites before unlinking and then verifies the files are gone, reporting anything it could not remove instead of assuming success.

_No parameters._

## `press`

Press a key or shortcut on the focused element: Enter, Tab, Escape, Backspace, Delete, Arrow*, Home, End, PageUp, PageDown, Space, or a single character. Combine with modifiers for shortcuts (e.g. key='a', modifiers=['ctrl']).

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Seconds to wait for the page to react (default 3) |
| `key` | string | yes | Key name or single character |
| `modifiers` | array |  | Any of alt, ctrl, meta, shift |

## `hover`

Move the real cursor over an element without clicking. Needed for menus and tooltips that only appear on a trusted mouseover — a JS-dispatched event is not isTrusted and many libraries check.

| param | type | required | description |
|---|---|---|---|
| `backend_node_id` | integer |  | backendNodeId from `find` |
| `budget_s` | number |  | Seconds to wait for the page to react (default 2) |
| `ref` | string |  | Stable reference from `observe` (preferred) |
| `selector` | string |  | CSS selector |

## `click_variant`

Double-click or right-click an element with real mouse events, using the same scroll and overlay checks as `click`.

| param | type | required | description |
|---|---|---|---|
| `backend_node_id` | integer |  | backendNodeId from `find` |
| `budget_s` | number |  | Seconds to wait for the page to react (default 4) |
| `kind` | string |  | double | right (default double) |
| `ref` | string |  | Stable reference from `observe` (preferred) |
| `selector` | string |  | CSS selector |

## `set_control`

Set a checkbox, radio, <select> or contenteditable. Goes through the framework-visible property setter, so React/Vue state updates too — assigning `.checked` directly changes the pixels while the app keeps the old value.

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Seconds to wait for the page to react (default 3) |
| `selector` | string | yes | CSS selector of the control |
| `value` | string | yes | For select: option value or visible text. For checkbox/radio: true|false. For contenteditable: the text |

## `drag`

Drag one element onto another with real mouse events, including the intermediate moves HTML5 drag-and-drop and JS drag libraries require (a press-then-release does nothing).

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Seconds to wait for the page to react (default 4) |
| `from_selector` | string | yes | CSS selector of the element to drag |
| `to_selector` | string | yes | CSS selector of the drop target |

## `perf_trace`

Web Vitals (LCP, CLS, TTFB), navigation timing, the slowest resources, and JS heap use — with an `insights` list naming which numbers exceed Google's published thresholds rather than leaving you to read a timing table.

_No parameters._

## `computed_style`

Resolved CSS for one element, its box, and — when it is not visible — an explicit `hidden_because` list (display:none / visibility:hidden / opacity:0 / zero-sized). Answers 'why can't I click this'.

| param | type | required | description |
|---|---|---|---|
| `properties` | array |  | Specific CSS properties; omit for a useful default set |
| `selector` | string | yes | CSS selector |

## `har_export`

Export captured network activity as a HAR 1.2 document, readable by DevTools and any HTTP analysis tool. URLs are redacted, since a HAR is routinely attached to a bug report.

| param | type | required | description |
|---|---|---|---|
| `limit` | integer |  | Maximum requests to include (default 200) |

## `trace_bundle`

The evidence bundle for this session: the ordered timeline of tool calls, policy decisions and walls, with secrets redacted. Shareable as-is; also written to disk on exit for `neobrowser trace open <id>`.

_No parameters._

## `login_flow`

Navigate to a login page, fill credentials, submit, and verify the result — in one call instead of five. Reports `needs_human` when it lands on an MFA or captcha step rather than claiming success. The internal steps are returned, so nothing is hidden.

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Total seconds for the whole flow (default 30) |
| `email` | string | yes | Username or email |
| `password` | string | yes | Password (never logged, never traced) |
| `url` | string | yes | https:// login page URL |

## `extract_paginated`

Extract a table or list across pages: extract, click next, repeat until the content stops changing or max_pages is reached. Stops on a repeated page rather than looping, and reports how many pages it actually covered.

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Total seconds for the whole crawl (default 60) |
| `max_pages` | integer |  | Safety cap (default 10, max 100) |
| `next_selector` | string | yes | CSS selector of the 'next page' control |
| `selector` | string | yes | CSS selector of the table or list container |

## `profile_mode`

Report which of the three session modes is active — isolated (ephemeral, no credentials), agent (persistent NeoBrowser profile you log into once), or attached (driving a Chrome you already have open) — plus what each implies for your credentials.

_No parameters._

## `pierce`

Reach an element inside shadow DOM or a same-origin iframe, where an ordinary selector returns null. Walks open shadow roots and reachable frames, and reports the path it found the element through. Cross-origin frames are skipped and listed by `list_frames` instead of silently missed.

| param | type | required | description |
|---|---|---|---|
| `action` | string |  | read | click | fill (default read) |
| `budget_s` | number |  | Seconds to wait for the page to react on click/fill (default 3) |
| `selector` | string | yes | CSS selector to find, at any depth |
| `value` | string |  | Value, for action=fill |

## `list_frames`

List every frame in the page with its URL and whether JS can reach into it. A cross-origin frame is reported as such, so an element you cannot find is explainable instead of a mystery — navigate to that frame's URL directly.

_No parameters._

## `dialog`

Answer a blocking alert/confirm/prompt/beforeunload. A JavaScript dialog freezes the renderer, so every other call hangs until it is answered — if the browser seems dead after a click, this is the first thing to try.

| param | type | required | description |
|---|---|---|---|
| `action` | string |  | accept | dismiss (default dismiss) |
| `prompt_text` | string |  | Text to enter, for a prompt() dialog |

## `emulate`

Override geolocation, viewport size, or grant browser permissions — what a location-gated or mobile-only flow needs before it will render at all. Pass latitude+longitude, width+height, and/or permissions (geolocation, camera, microphone, notifications, clipboard).

| param | type | required | description |
|---|---|---|---|
| `height` | integer |  | Viewport height in CSS pixels |
| `latitude` | number |  | Latitude to report |
| `longitude` | number |  | Longitude to report |
| `mobile` | boolean |  | Emulate a mobile device (sets a 2x scale factor; default false) |
| `permissions` | array |  | Permissions to grant |
| `width` | integer |  | Viewport width in CSS pixels |

## `bridge_status`

Whether the NeoBrowser Bridge extension is connected, and which tabs the user has shared with this agent. Use it before assuming a bridge tab is drivable: sharing is per-tab and the user can revoke at any time.

_No parameters._

## `bridge_cdp`

Send one CDP command to a tab the user shared through the bridge extension — their real browser, their real session, nothing cloned. The extension refuses any tab that is not currently shared, so this cannot reach the rest of their browser.

| param | type | required | description |
|---|---|---|---|
| `method` | string | yes | CDP method, e.g. Runtime.evaluate |
| `params` | object |  | CDP params object |
| `tab_id` | integer | yes | Chrome tab id, from bridge_status.shared_tabs |

## `cpu_profile`

Sample the JS main thread and report the functions that burned the time (self-time, ranked). Interact with the page while this runs. Returns the top 20 rather than the raw sample tree, which is tens of thousands of nodes.

| param | type | required | description |
|---|---|---|---|
| `duration_ms` | integer |  | Sampling window, 100–30000 (default 3000) |

## `heap_stats`

JS heap size plus DOM node, listener, document and frame counts. Call it twice around a repeated interaction: counts that grow every cycle and never come back down is the signature of a leak.

_No parameters._

## `source_map`

Trace a minified stack frame to its original file, line and column by decoding the script's source map. Pass the line/column from the stack trace (1-based, as stacks report them). Reports null with an explanation when no mapping covers that position, rather than guessing a nearby line.

| param | type | required | description |
|---|---|---|---|
| `column` | integer |  | Column in the minified file |
| `line` | integer |  | Line in the minified file |
| `script_url` | string | yes | URL of the minified script |

## `har_import`

Read a HAR document (from DevTools, a colleague, a bug report) and summarise its failures and slowest requests. Summarised rather than echoed, since a HAR is megabytes; URLs are redacted, since someone else's HAR is full of their tokens.

| param | type | required | description |
|---|---|---|---|
| `path` | string | yes | Path to a .har file |

## `navigate`

Open URL in Chrome (tab reuse, self-healing). Required for SPAs, JS-heavy sites, and login-required pages.

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Total seconds allowed for the navigation (default 15). Returns status 'uncertain' if the page is not ready in time rather than waiting longer |
| `url` | string | yes | HTTP/HTTPS URL to open |
| `wait_s` | number |  | Deprecated alias kept for compatibility: raises budget_s if larger |

## `read`

Extract visible text from the current page via JavaScript.

| param | type | required | description |
|---|---|---|---|
| `raw` | boolean |  | Return the bare text without the untrusted-content fence (default false). Only for scripted extraction — a model should read the fenced form |
| `selector` | string |  | Optional CSS selector to read a specific element (default: body) |

## `screenshot`

Capture current page viewport as a base64 image (PNG default, or JPEG).

| param | type | required | description |
|---|---|---|---|
| `format` | string |  | Image format: png (default) or jpeg |
| `quality` | integer |  | JPEG quality 0-100 (default 80, ignored for PNG) |

## `find`

Find a UI element by natural-language intent (accessibility tree + heuristics). Returns a backendNodeId for use with click.

| param | type | required | description |
|---|---|---|---|
| `intent` | string | yes | What to find, e.g. 'send button', 'message input box' |

## `click`

Click an element by backendNodeId (from find) or CSS selector. Uses real (isTrusted) mouse events. Scrolls the target into view first, and refuses to click when another element (modal, cookie banner, sticky header) covers it — reporting which one, so you can dismiss it and retry rather than assuming the click landed.

| param | type | required | description |
|---|---|---|---|
| `backend_node_id` | integer |  | backendNodeId from a find result. Valid only until the node is recreated |
| `budget_s` | number |  | Seconds to wait for the page to react before reporting 'uncertain' (default 5) |
| `ref` | string |  | Stable reference from `observe` (e.g. `button:Continue#0`). PREFERRED: it is re-resolved against the live tree on every use, so it still works after a re-render |
| `selector` | string |  | CSS selector fallback |

## `type`

Type text into the focused element. Default: instant insert (React/Vue-safe). Set human=true for per-key events with human cadence.

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Seconds to wait for the field to reflect the change before reporting 'uncertain' (default 3) |
| `human` | boolean |  | Type key-by-key with human-like timing (default false) |
| `text` | string | yes | Text to type |

## `js`

Execute JavaScript in the page. Use a return statement to get a value; `await` is supported.

| param | type | required | description |
|---|---|---|---|
| `code` | string | yes | JavaScript code to execute. Must use a return statement to return a value. |

## `page_info`

Summarize the page: url, title, interactive-element count, forms, and overlay presence.

_No parameters._

## `analyze`

Structured page analysis: forms with fields+labels, buttons, overlays, and the active element.

_No parameters._

## `fill`

Fill a field by CSS selector (input/textarea/select/checkbox/radio/contenteditable), firing input+change.

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Seconds to wait for the field to reflect the change before reporting 'uncertain' (default 3) |
| `selector` | string | yes | CSS selector for the field |
| `value` | string | yes | Value to fill |

## `form_fill`

Fill multiple fields by fuzzy label/name/placeholder/aria match.

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Seconds to wait for the form to reflect the changes before reporting 'uncertain' (default 4) |
| `fields` | object | yes | Dict of {label_or_placeholder: value} pairs |
| `form_index` | integer |  | Which form to target (default 0) |

## `submit`

Submit a form: click the given selector or auto-detect a submit control, then wait for navigation.

| param | type | required | description |
|---|---|---|---|
| `selector` | string |  | Optional CSS selector of the submit control |
| `wait_s` | number |  | Max seconds to wait for navigation (default 5.0) |

## `find_and_click`

Click the nth VISIBLE clickable element whose text or aria-label contains the given text. Hidden and collapsed matches (closed accordion steps, header panels duplicating a body form) are skipped and counted in matched_total vs matched_visible, so a multi-step form can't silently submit the wrong step.

| param | type | required | description |
|---|---|---|---|
| `budget_s` | number |  | Seconds to wait for the page to react before reporting 'uncertain' (default 5) |
| `nth` | integer |  | Which match to click (0-based, default 0) |
| `role` | string |  | Optional ARIA role to narrow the search |
| `text` | string | yes | Visible text or label to search for |

## `dismiss_overlay`

Detect and dismiss cookie/GDPR/newsletter overlays by clicking accept/close inside them. force=true also tries Escape + backdrop.

| param | type | required | description |
|---|---|---|---|
| `force` | boolean |  | Also try Escape + backdrop click (default false) |

## `extract`

Extract structured data: 'links' (default) returns anchors as text+href; 'tables' returns table outerHTML.

| param | type | required | description |
|---|---|---|---|
| `what` | string |  | What to extract: links (default) or tables |

## `extract_table`

Parse a table into an array of {header: cell} row objects.

| param | type | required | description |
|---|---|---|---|
| `index` | integer |  | Which matched table to parse (default 0) |
| `selector` | string |  | CSS selector for the table(s) (default: table) |

## `scroll`

Scroll the viewport (up/down/top/bottom), then force a frame so load-on-scroll content paints.

| param | type | required | description |
|---|---|---|---|
| `amount` | integer |  | Pixels to scroll for up/down (default 500) |
| `direction` | string |  | up, down (default), top, or bottom |

## `wait`

Wait for a fixed time, or (if selector given) poll until it appears, up to ms.

| param | type | required | description |
|---|---|---|---|
| `ms` | integer |  | Milliseconds to wait / poll timeout (default 1000) |
| `selector` | string |  | Optional CSS selector to wait for |

## `paginate`

Advance to the next page: click a given selector or auto-detect a next/more control, then force a frame.

| param | type | required | description |
|---|---|---|---|
| `selector` | string |  | Optional CSS selector of the next control |

## `console_logs`

Get captured browser console entries (log/info/warning/error/exception).

| param | type | required | description |
|---|---|---|---|
| `level` | string |  | Filter by level: log, info, warning, error (default: all) |
| `limit` | integer |  | Max entries to return (default 50) |

## `network_log`

Get captured network requests with status, duration, and size.

| param | type | required | description |
|---|---|---|---|
| `limit` | integer |  | Max entries (default 50) |
| `url_pattern` | string |  | Filter by URL substring (default: all) |

## `metrics`

Get Chrome performance metrics: JSHeapUsedSize, Nodes, Documents, etc.

| param | type | required | description |
|---|---|---|---|
| `key` | string |  | Return only this metric (default: all) |

## `debug`

Control an in-page console interceptor: start (install), flush (default, drain captured logs), stop (remove).

| param | type | required | description |
|---|---|---|---|
| `action` | string |  | start, flush (default), or stop |

## `save_cookies`

Save the current session's cookies to ~/.neobrowser/cookies/{profile}.json (0600 perms).

_No parameters._

## `restore_cookies`

Inject saved cookies from disk into the current tab. Returns count restored.

_No parameters._

## `save_session`

Full session save: cookies + localStorage → ~/.neobrowser/sessions/. Persists authenticated state across restarts.

_No parameters._

## `session_info`

Show session persistence state: last save time, cookie count, domains, file paths.

_No parameters._

## `login`

Navigate an https login page, fill email + password, submit, and report honest success (a lingering password field means it failed).

| param | type | required | description |
|---|---|---|---|
| `email` | string | yes | Email or username |
| `password` | string | yes | Password |
| `url` | string | yes | Login page URL (must be https) |

## `browse`

Server-side fetch of a public URL (SSRF-guarded). JSON passes through; HTML is reduced to text. Does not use the browser tab.

| param | type | required | description |
|---|---|---|---|
| `headers` | object |  | Optional extra request headers |
| `url` | string | yes | Public http/https URL to fetch |

## `upload`

Attach local file(s) to a file input by CSS selector. Files must live under an allowed directory (~/Downloads, ~/Desktop, ~/Documents, ~/.neobrowser/downloads, or NEOBROWSER_UPLOAD_DIR) and never a sensitive path (ssh/aws keys, .env, keychains, credentials).

| param | type | required | description |
|---|---|---|---|
| `files` | array | yes | Absolute file path(s) to attach |
| `selector` | string | yes | CSS selector of the file input |

## `download`

Download a public URL to ~/.neobrowser/downloads/, reusing the tab's cookies for auth-gated files (SSRF-guarded, 200MB cap).

| param | type | required | description |
|---|---|---|---|
| `filename` | string |  | Optional output filename |
| `url` | string | yes | Direct file URL to download |

## `search`

Web text search through the real browser: Google first (uses your logged-in profile if set), DuckDuckGo fallback.

| param | type | required | description |
|---|---|---|---|
| `limit` | integer |  | Max results (default 10) |
| `query` | string | yes | Search query |

## `search_images`

Google Images search: returns image_url, source, title, and a curl download command.

| param | type | required | description |
|---|---|---|---|
| `count` | integer |  | Max results (default 10, max 30) |
| `query` | string | yes | Image search query |

## `search_videos`

Google Videos search: returns url, channel, duration, platform, and a yt-dlp download command.

| param | type | required | description |
|---|---|---|---|
| `count` | integer |  | Max results (default 10, max 30) |
| `query` | string | yes | Video search query |

## `search_twitter_videos`

Search for videos on X/Twitter (video search scoped to x.com/twitter.com).

| param | type | required | description |
|---|---|---|---|
| `count` | integer |  | Max results (default 10, max 30) |
| `query` | string | yes | Twitter video search query |

## `record_task`

Start recording interaction steps as a playbook for later replay.

| param | type | required | description |
|---|---|---|---|
| `domain` | string | yes | Domain key, e.g. 'linkedin.com' |
| `task_name` | string | yes | Task identifier, e.g. 'send_message' |

## `stop_recording`

Stop recording and save the playbook. Returns the number of steps captured.

_No parameters._

## `replay`

Replay a recorded playbook by re-invoking each saved step. Returns ok + the first failed step index (0 = none).

| param | type | required | description |
|---|---|---|---|
| `domain` | string | yes | Domain key |
| `task_name` | string | yes | Task name |

## `new_tab`

Open a new browser tab on the shared Chrome and make it active. Returns its index/id.

_No parameters._

## `list_tabs`

List open tabs with index, url, title, and which is active.

_No parameters._

## `switch_tab`

Make the tab at the given index active (subsequent tools act on it).

| param | type | required | description |
|---|---|---|---|
| `index` | integer | yes | Tab index from list_tabs |

## `close_tab`

Close the tab at the given index (cannot close the last remaining tab).

| param | type | required | description |
|---|---|---|---|
| `index` | integer | yes | Tab index from list_tabs |

